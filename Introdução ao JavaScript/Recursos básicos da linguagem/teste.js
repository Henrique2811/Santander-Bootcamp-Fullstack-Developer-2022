var preco = 2;
var desconto = 0.2;
const PRECO = 2; 
var total = PRECO - desconto;


function soma(a, b) {
    console.log(a + b) //Apenas ver o valor no console para ter certeza de que aquilo está funcionando
    return a + b; //Quando não precisa retornar e manipular o resultado de uma função não precisa utilizar o return
}

function soma(a, b) {
    return a + b; // 802
}

soma(3, 5);